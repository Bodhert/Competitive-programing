#include <stack>
#include <iostream>
#include <stdio.h>

using namespace std;

int main(){
  string in1, in2;
  char value,suit;
  while(getline(cin,in) && in != "#"){
    getline(cin,in2);
    //    cout << in << in2 << endl;
    
  }
   
  return 0;
}
