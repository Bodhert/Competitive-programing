#include <iostream>
#include <stdlib.h>
#include <cstdio>

int main(){
  double cars, cows,shows,dors,fisrtP,secondP;
  while(scanf("%d %d %d",&cows,&cars,&shows ) != EOF){
    dors = cars+cows;
    fistP = (cows/dors) * (cows/(dors - shows -1))
      
    
  }
}